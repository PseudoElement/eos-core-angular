trigger:
  batch: 'true'
  branches:
    include:
    - dev
        
pool:
  name: Default

steps:
- task: PowerShell@2
  displayName: Build
  inputs:
    targetType: filePath
    filePath: ./build.ps1
