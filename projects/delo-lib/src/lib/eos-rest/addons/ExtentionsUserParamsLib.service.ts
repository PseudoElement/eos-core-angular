import { Injectable } from '@angular/core';
@Injectable()
export class ExetentionsUserParamsLib {
    constructor() { }
    public loadDepartments(value): Promise<any> {
        return Promise.resolve(null);
    }
}